{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="bg-primary-500 hover:bg-primary-600 text-white font-bold py-2 px-4 rounded-lg shadow-md hover:shadow-lg transition-all mt-4">{{ button_label|default('Guardar') }}</button>
{{ form_end(form) }}