<form method="post" action="{{ path('app_admin_fuel_type_delete', {'id': fuel_type.id}) }}" onsubmit="return confirm('¿Estás seguro de que quieres eliminar este tipo de combustible?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fuel_type.id) }}">
    <button class="text-red-500 hover:text-red-800">Eliminar</button>
</form>