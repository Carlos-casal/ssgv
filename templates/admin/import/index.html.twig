{% extends 'base.html.twig' %}

{% block page_title %}Importar Horas desde CSV{% endblock %}
{% block page_header %}
    <h1 class="text-2xl font-bold text-gray-900">Importar Horas desde CSV</h1>
{% endblock %}

{% block main %}
    <div class="bg-white p-6 rounded-lg shadow-md">
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} mb-4 p-4 rounded-md {% if label == 'success' %}bg-green-100 text-green-800{% else %}bg-red-100 text-red-800{% endif %}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}

        <p class="mb-4 text-gray-700">
            Sube un archivo CSV para importar las horas de los voluntarios. El archivo debe tener las siguientes columnas:
            <code class="bg-gray-200 p-1 rounded">N° VOLUNTARIO</code>,
            <code class="bg-gray-200 p-1 rounded">HORAS_1</code>,
            <code class="bg-gray-200 p-1 rounded">FECHA_1</code>,
            <code class="bg-gray-200 p-1 rounded">COMENTARIO_1</code>, etc.
        </p>

        <form method="post" enctype="multipart/form-data" class="space-y-4">
            <div>
                <label for="csv_file" class="block text-sm font-medium text-gray-700">Archivo CSV</label>
                <input type="file" name="csv_file" id="csv_file" required
                       class="mt-1 block w-full text-sm text-gray-900 border border-gray-300 rounded-lg cursor-pointer bg-gray-50 focus:outline-none">
            </div>

            <div>
                <label for="year" class="block text-sm font-medium text-gray-700">Año</label>
                <input type="number" name="year" id="year" required min="2000" max="{{ "now"|date("Y") }}" value="{{ "now"|date("Y") }}"
                       class="mt-1 block w-full p-2 border border-gray-300 rounded-lg focus:ring-blue-500 focus:border-blue-500">
            </div>

            <div>
                <button type="submit"
                        class="px-4 py-2 bg-blue-600 text-white font-semibold rounded-lg shadow-md hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-75">
                    Importar
                </button>
            </div>
        </form>
    </div>
{% endblock %}
