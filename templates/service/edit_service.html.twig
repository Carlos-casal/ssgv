{% extends 'layout/app.html.twig' %}

{% block title %}Editar Servicio{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link href="https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.snow.css" rel="stylesheet">
    <style>
        #quill-editor-edit, #tasks-editor-edit {
            height: 250px;
        }
    </style>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script src="https://cdn.jsdelivr.net/npm/quill@2.0.2/dist/quill.js"></script>
    <script>
        const Link = Quill.import('formats/link');
        class CustomLink extends Link {
            static sanitize(url) {
                const sanitizedUrl = super.sanitize(url);
                if (sanitizedUrl && sanitizedUrl !== 'about:blank' && !/^(https?:\/\/|mailto:|tel:)/.test(sanitizedUrl)) {
                    return 'https://' + sanitizedUrl;
                }
                return sanitizedUrl;
            }
        }
        Quill.register(CustomLink, true);

        const quillInstances = {};
        function setupQuill(containerId, textareaId) {
            const editorContainer = document.querySelector(containerId);
            const hiddenTextarea = document.querySelector(textareaId);
            if (!editorContainer || !hiddenTextarea || quillInstances[containerId]) return;

            const toolbarOptions = [
                [{ 'header': [1, 2, 3, false] }],
                ['bold', 'italic', 'strike', 'underline'],
                [{ 'script': 'sub'}, { 'script': 'super' }],
                [{ 'list': 'ordered'}, { 'list': 'bullet' }],
                [{ 'align': [] }],
                ['link'],
                ['clean']
            ];
            const quill = new Quill(editorContainer, {
                modules: { toolbar: toolbarOptions },
                theme: 'snow'
            });
            quill.root.innerHTML = hiddenTextarea.value;
            quill.on('text-change', () => {
                hiddenTextarea.value = quill.root.innerHTML;
            });
            quillInstances[containerId] = quill;
        }

        function initializeAllQuillEditors() {
            setupQuill('#quill-editor-edit', '#description_textarea_edit');
            setupQuill('#tasks-editor-edit', '#tasks_textarea_edit');
        }

        document.addEventListener('turbo:before-cache', () => {
            for (const key in quillInstances) {
                if (quillInstances.hasOwnProperty(key) && quillInstances[key]) {
                    quillInstances[key] = null;
                }
            }
        });
        document.addEventListener('turbo:load', initializeAllQuillEditors);
        document.addEventListener('DOMContentLoaded', initializeAllQuillEditors);
    </script>
{% endblock %}

{% block content %}
    <div class="container mx-auto p-6">
        <h1 class="text-2xl font-bold mb-6 text-gray-900">Editar Servicio</h1>
        {# ... flash messages ... #}
        <ul class="nav nav-tabs" id="myTab" role="tablist">
            <li class="nav-item"><a class="nav-link active" id="datos_basicos-tab" data-toggle="tab" href="#datos_basicos" role="tab">Datos básicos</a></li>
            <li class="nav-item"><a class="nav-link" id="asistencias-tab" data-toggle="tab" href="#asistencias" role="tab">Confirmaciones</a></li>
        </ul>
        <div class="tab-content" id="myTabContent">
            <div class="tab-pane fade show active" id="datos_basicos" role="tabpanel">
                {{ form_start(form, {'attr': {'class': 'bg-white p-6 rounded-lg shadow-md'}}) }}
                <div class="space-y-6">

                    {# Fila 1 #}
                    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                        <div>{{ form_row(form.numeration) }}</div>
                        <div>{{ form_row(form.title) }}</div>
                        <div>{{ form_row(form.locality, {'label': 'Lugar 📍'}) }}</div>
                    </div>

                    {# Fila 2 #}
                    <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                        <div>{{ form_row(form.startDate) }}</div>
                        <div>{{ form_row(form.timeAtBase) }}</div>
                        <div>{{ form_row(form.departureTime) }}</div>
                        <div>{{ form_row(form.endDate) }}</div>
                    </div>

                    {# Fila 3 #}
                    <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                        <div>{{ form_row(form.registrationLimitDate) }}</div>
                        <div>{{ form_row(form.maxAttendees) }}</div>
                        <div>{{ form_row(form.type) }}</div>
                        <div>{{ form_row(form.category) }}</div>
                    </div>

                    {# Fila 4 - Recursos #}
                    <div>
                        <h3 class="text-lg font-semibold mb-4 border-b pb-2">Recursos</h3>
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
                            <div class="space-y-4">
                                <label class="block font-medium">Ambulancias</label>
                                <div>{{ form_row(form.numSvb, {'label': 'SVB 🚑'}) }}</div>
                                <div>{{ form_row(form.numSva, {'label': 'SVA 🚑'}) }}</div>
                                <div>{{ form_row(form.numSvae, {'label': 'SVAE 🚑'}) }}</div>
                            </div>
                            <div class="space-y-4">
                                <label class="block font-medium">Personal Sanitario</label>
                                <div>{{ form_row(form.numMedico, {'label': 'Médico 🥼'}) }}</div>
                                <div>{{ form_row(form.numEnfermero, {'label': 'Enfermería 🩺'}) }}</div>
                            </div>
                            <div class="space-y-4 pt-8">
                               <div>{{ form_row(form.afluencia, {'label': 'Afluencia 📈'}) }}</div>
                               <div class="pt-4">{{ form_row(form.hasFieldHospital, {'label': 'Hospital de Campaña 🏥'}) }}</div>
                               <div class="pt-2">{{ form_row(form.hasProvisions, {'label': 'Avituallamiento 🥪'}) }}</div>
                            </div>
                        </div>
                    </div>

                    {# Fila 5 - Tareas y Descripción #}
                    <div class="space-y-6">
                        <div>
                            {{ form_label(form.tasks) }}
                            <div id="tasks-editor-edit"></div>
                            {{ form_widget(form.tasks, {'id': 'tasks_textarea_edit', 'attr': {'style': 'display:none;'}}) }}
                            {{ form_errors(form.tasks) }}
                        </div>
                        <div>
                            {{ form_label(form.description) }}
                            <div id="quill-editor-edit"></div>
                            {{ form_widget(form.description, {'id': 'description_textarea_edit', 'attr': {'style': 'display:none;'}}) }}
                            {{ form_errors(form.description) }}
                        </div>
                    </div>

                     {# Fila 6 - Recipients #}
                    <div>
                        <h3 class="text-lg font-semibold mb-2 border-b pb-1">Destinatarios</h3>
                        {{ form_row(form.recipients) }}
                    </div>
                </div>

                <div class="mt-8 text-center">
                    <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg">Guardar Cambios</button>
                </div>
                {{ form_end(form) }}
            </div>
            <div class="tab-pane fade" id="asistencias" role="tabpanel">
                {# Asistencias content #}
            </div>
        </div>
    </div>
{% endblock %}
