{% extends 'layout/app.html.twig' %}

{% block title %}Editar Servicio: {{ service.title }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap');

        body {
            font-family: 'Inter', sans-serif;
        }

        .form-error {
            color: var(--ui-danger);
            font-size: 0.75rem;
            margin-top: 0.25rem;
            font-weight: 600;
        }

        /* iPhone Switch Styling */
        .form-switch .form-check-input {
            width: 2.5em;
            height: 1.25em;
            cursor: pointer;
        }
        .form-switch .form-check-input:checked {
            background-color: var(--ui-accent);
            border-color: var(--ui-accent);
        }

        .afluencia-baja { color: #16a34a !important; font-weight: 700; }
        .afluencia-media { color: #ea580c !important; font-weight: 700; }
        .afluencia-alta { color: #dc2626 !important; font-weight: 900; }
    </style>
{% endblock %}

{% block page_title %}{% endblock %}

{% block content %}
<div class="animate-in" data-controller="service-form" data-service-id="{{ service.id }}">
    {{ form_start(form, {
        'attr': {
            'id': 'edit-service-form-element',
            'data-service-form-target': 'form',
            'novalidate': 'novalidate'
        }
    }) }}

    <div class="mb-6 flex flex-col lg:flex-row justify-between items-start lg:items-end gap-4">
        <div>
            <h1 class="text-2xl font-black text-slate-900 dark:text-white mb-1">Editar Servicio</h1>
            <p class="text-sm text-slate-500 dark:text-slate-400">Actualizar planificación de {{ service.title }}</p>
        </div>
        <div class="flex gap-2 w-full lg:w-auto">
            <a href="{{ path('app_service_show', {id: service.id}) }}" class="ui-btn btn-dark flex-1 lg:flex-none">
                <i data-lucide="x" class="w-4 h-4 mr-2"></i> Cancelar
            </a>
            <button type="button" data-action="click->service-form#submitForm" class="ui-btn btn-cyan flex-1 lg:flex-none">
                <i data-lucide="save" class="w-4 h-4 mr-2"></i> GUARDAR CAMBIOS
            </button>
        </div>
    </div>

    <!-- Navigation Tabs -->
    <ul class="nav nav-tabs nav-tabs-custom border-b border-slate-200 dark:border-slate-800 mb-4 bg-slate-50/80 dark:bg-slate-900/80 backdrop-blur-md -mx-6 px-6" style="z-index: 100;" role="tablist">
        <li class="nav-item" role="presentation">
            <button class="nav-link active" id="datos-tab" data-bs-toggle="tab" data-bs-target="#datos" type="button" role="tab" aria-controls="datos" aria-selected="true">
                <i data-lucide="file-text" class="w-4 h-4 inline me-2"></i> Datos Generales
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="recursos-tab" data-bs-toggle="tab" data-bs-target="#recursos" type="button" role="tab" aria-controls="recursos" aria-selected="false">
                <i data-lucide="truck" class="w-4 h-4 inline me-2"></i> Recursos y Dotación
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="asistencia-tab" data-bs-toggle="tab" data-bs-target="#asistencia" type="button" role="tab" aria-controls="asistencia" aria-selected="false">
                <i data-lucide="users" class="w-4 h-4 inline me-2"></i> Asistencia ({{ service.assistanceConfirmations|length }})
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="vehiculos-tab" data-bs-toggle="tab" data-bs-target="#vehiculos_dotacion" type="button" role="tab" aria-controls="vehiculos_dotacion" aria-selected="false">
                <i data-lucide="truck" class="w-4 h-4 inline me-2"></i> Vehículos y Dotaciones
            </button>
        </li>
        <li class="nav-item" role="presentation">
            <button class="nav-link" id="registro-tab" data-bs-toggle="tab" data-bs-target="#registro_partes" type="button" role="tab" aria-controls="registro_partes" aria-selected="false">
                <i data-lucide="clipboard-list" class="w-4 h-4 inline me-2"></i> Registro y Partes
            </button>
        </li>
    </ul>

    <div class="tab-content" id="serviceFormTabContent">
        <!-- TAB 1: DATOS GENERALES -->
        <div class="tab-pane fade show active" id="datos" role="tabpanel" aria-labelledby="datos-tab">
            <!-- Título y Ubicación (Ancho Completo) -->
            <div class="ui-fieldset !pb-4 !mb-4">
                <div class="ui-legend">Identificación y Ubicación</div>
                <div class="grid grid-cols-1 lg:grid-cols-12 gap-4">
                    <div class="lg:col-span-6">
                        <div class="field-group !mb-2">
                            <label class="field-label">Título del Servicio <span>*</span></label>
                            {{ form_widget(form.title, {'attr': {'class': 'ui-input font-bold', 'placeholder': 'Ej. Dispositivo Maratón', 'data-action': 'input->service-form#clearError', 'data-service-form-target': 'title'}}) }}
                        </div>
                    </div>
                    <div class="lg:col-span-2">
                        <div class="field-group !mb-2">
                            <label class="field-label">Nº Registro</label>
                            {{ form_widget(form.numeration, {'attr': {'class': 'ui-input font-mono !bg-slate-50 dark:!bg-slate-900', 'placeholder': 'ID', 'data-service-form-target': 'numeration'}}) }}
                        </div>
                    </div>
                    <div class="lg:col-span-4">
                        <div class="field-group !mb-2">
                            <label class="field-label">Ubicación / Lugar <span>*</span></label>
                            <div class="ui-input-wrapper">
                                <i data-lucide="map-pin" class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400"></i>
                                {{ form_widget(form.locality, {'attr': {'class': 'ui-input !pl-11', 'placeholder': 'Dirección...'}}) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="grid grid-cols-1 lg:grid-cols-12 gap-4">
                <!-- COLUMNA IZQUIERDA: Cronología -->
                <div class="lg:col-span-8">
                    <div class="ui-fieldset !pb-4 h-full">
                        <div class="ui-legend">Cronología del Servicio</div>
                        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
                            <div class="lg:col-span-2">
                                <div class="grid grid-cols-2 gap-4">
                                    <div class="date-input-container">
                                        <span class="date-badge">INICIO</span>
                                        <div class="ui-input-wrapper">
                                            {{ form_widget(form.startDate, {'attr': {'class': 'ui-input', 'data-service-form-target': 'startDateInput'}}) }}
                                            <i data-lucide="calendar" class="ui-input-icon-right"></i>
                                        </div>
                                    </div>
                                    <div class="date-input-container">
                                        <span class="date-badge">FIN</span>
                                        <div class="ui-input-wrapper">
                                            {{ form_widget(form.endDate, {'attr': {'class': 'ui-input', 'data-service-form-target': 'endDateInput'}}) }}
                                            <i data-lucide="calendar" class="ui-input-icon-right"></i>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="date-input-container">
                                <span class="date-badge">H. EN BASE (REGRESO)</span>
                                <div class="ui-input-wrapper">
                                    {{ form_widget(form.timeAtBase, {'attr': {'class': 'ui-input', 'data-service-form-target': 'timeAtBaseInput'}}) }}
                                    <i data-lucide="clock" class="ui-input-icon-right"></i>
                                </div>
                            </div>
                            <div class="date-input-container">
                                <span class="date-badge">HORA SALIDA</span>
                                <div class="ui-input-wrapper">
                                    {{ form_widget(form.departureTime, {'attr': {'class': 'ui-input', 'data-service-form-target': 'departureTimeInput'}}) }}
                                    <i data-lucide="clock" class="ui-input-icon-right"></i>
                                </div>
                            </div>
                            <div class="lg:col-span-2">
                                <div class="date-input-container !pt-8">
                                    <span class="date-badge">LÍMITE INSCRIPCION</span>
                                    <div class="ui-input-wrapper">
                                        {{ form_widget(form.registrationLimitDate, {'attr': {'class': 'ui-input', 'data-service-form-target': 'registrationLimitInput'}}) }}
                                        <i data-lucide="calendar" class="ui-input-icon-right"></i>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- COLUMNA DERECHA: Clasificación -->
                <div class="lg:col-span-4">
                    <div class="ui-fieldset !pb-4 h-full">
                        <div class="ui-legend">Clasificación</div>
                        <div class="space-y-4">
                            <div class="field-group !mb-0">
                                <label class="field-label">Tipo / Categoría <span>*</span></label>
                                <div class="flex mb-2">
                                    {{ form_widget(form.type, {'attr': {'class': 'ui-input !rounded-r-none border-r-0', 'data-action': 'change->service-form#updateCategories', 'data-service-form-target': 'typeSelect'}}) }}
                                    <button type="button" class="ui-btn btn-cyan !px-3 !rounded-l-none" data-action="click->service-form#openTypeModal">
                                        <i data-lucide="plus" class="w-4 h-4"></i>
                                    </button>
                                </div>
                                <div class="flex">
                                    {{ form_widget(form.subcategory, {'attr': {'class': 'ui-input !rounded-r-none border-r-0', 'data-service-form-target': 'subcategorySelect'}}) }}
                                    <button type="button" class="ui-btn btn-cyan !px-3 !rounded-l-none" data-action="click->service-form#openSubcategoryModal">
                                        <i data-lucide="plus" class="w-4 h-4"></i>
                                    </button>
                                </div>
                            </div>
                            <div class="field-group !mb-0">
                                <div class="grid grid-cols-2 gap-2">
                                    <div>
                                        <label class="field-label">Afluencia <span>*</span></label>
                                        {{ form_widget(form.afluencia, {'attr': {'class': 'ui-input', 'data-action': 'change->service-form#updateAfluenciaColor', 'data-service-form-target': 'afluenciaSelect'}}) }}
                                    </div>
                                    <div>
                                        <label class="field-label">Nº Personas</label>
                                        {{ form_widget(form.estimatedPeople, {'attr': {'class': 'ui-input text-center', 'placeholder': 'Aprox.', 'data-service-form-target': 'estimatedPeopleInput'}}) }}
                                    </div>
                                </div>
                            </div>
                            <div class="field-group !mb-0">
                                <label class="field-label">Límite Inscritos</label>
                                <div class="ui-input-wrapper">
                                    <i data-lucide="users" class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400"></i>
                                    {% set maxAttendeesWidget = form_widget(form.maxAttendees, {'attr': {'class': 'ui-input !pl-11', 'placeholder': 'Sin límite'}}) %}
                                    {{ maxAttendeesWidget|raw }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- FILA 2: Descripción y Tareas (Ancho Completo) -->
                <div class="lg:col-span-12">
                    <div class="ui-fieldset">
                        <div class="ui-legend">Definición Operativa</div>

                        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                            <div class="field-group !mb-0">
                                <label class="field-label">Tareas del Servicio</label>
                                {{ form_widget(form.tasks, {'attr': {'class': 'ui-input ui-textarea', 'style': 'height: 250px;', 'placeholder': 'Describe brevemente las tareas...', 'data-service-form-target': 'tasksInput'}}) }}
                            </div>
                            <div class="field-group !mb-0">
                                <label class="field-label">Descripción Detallada</label>
                                {{ form_widget(form.description, {'attr': {'class': 'ui-input ui-textarea', 'style': 'height: 250px;', 'data-service-form-target': 'descriptionInput'}}) }}
                            </div>
                        </div>
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-6 align-items-center">
                            <div class="flex justify-between items-center bg-slate-50 dark:bg-slate-900/50 p-3 rounded-xl border border-slate-100 dark:border-slate-800">
                                <span class="text-sm font-bold text-slate-700 dark:text-slate-300">Avituallamiento</span>
                                <div class="form-check form-switch m-0 p-0">
                                    {{ form_widget(form.hasProvisions, {'attr': {'class': 'form-check-input !mt-0'}}) }}
                                </div>
                            </div>
                            <div class="flex justify-between items-center bg-slate-50 dark:bg-slate-900/50 p-3 rounded-xl border border-slate-100 dark:border-slate-800">
                                <span class="text-sm font-bold text-slate-700 dark:text-slate-300">Hospital Campaña</span>
                                <div class="form-check form-switch m-0 p-0">
                                    {{ form_widget(form.hasFieldHospital, {'attr': {'class': 'form-check-input !mt-0'}}) }}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB 2: RECURSOS Y DOTACIÓN -->
        <div class="tab-pane fade" id="recursos" role="tabpanel" aria-labelledby="recursos-tab">
            <div class="row g-3">
                <div class="col-12">
                    <div class="card border-0 shadow-sm rounded-3">
                        <div class="card-header-compact bg-blue-50/50">
                            <div class="d-flex align-items-center">
                                <i data-lucide="package" class="w-4 h-4 text-blue-700 mr-2"></i>
                                <span class="section-title text-blue-700">Recursos, Materiales y Dotación</span>
                            </div>
                        </div>
                        <div class="card-body p-3">
                            <div class="row g-3">
                                <!-- Materiales -->
                                <div class="col-lg-8 border-end">
                                    <div class="row g-2" id="materials-container"
                                         data-service-form-target="materialsContainer"
                                         data-index="{{ form.serviceMaterials|length }}"
                                         data-prototype="{{ include('service/_material_prototype.html.twig', { 'form': form.serviceMaterials.vars.prototype })|e('html_attr') }}">

                                        {% for category in ['Sanitario', 'Comunicaciones', 'Logística', 'Otros'] %}
                                        <div class="col-xl-3 col-lg-6 col-md-6 mb-4">
                                            <div class="d-flex justify-content-between align-items-center mb-2 px-1">
                                                <span class="text-xxs font-black text-slate-400 uppercase tracking-tighter">{{ category }}</span>
                                                <button type="button" class="btn btn-link p-0 text-blue-600" data-action="click->service-form#addMaterial" data-category="{{ category }}">
                                                    <i data-lucide="plus-square" class="w-4 h-4"></i>
                                                </button>
                                            </div>
                                            <div class="space-y-2" data-material-category="{{ category }}">
                                                {% for sm in form.serviceMaterials %}
                                                    {% if sm.vars.data.material and sm.vars.data.material.category == category %}
                                                        {{ include('service/_material_prototype.html.twig', { 'form': sm }) }}
                                                    {% endif %}
                                                {% endfor %}
                                            </div>
                                        </div>
                                        {% endfor %}
                                    </div>
                                </div>

                                <!-- Dotación y Vehículos -->
                                <div class="col-lg-4">
                                    <div class="row g-2">
                                        <!-- Personal -->
                                        <div class="col-6">
                                            <span class="text-xxs font-black text-slate-400 uppercase tracking-tighter d-block mb-2">Personal Requerido</span>
                                            <div class="space-y-1">
                                                {% for field in ['numTes', 'numTts', 'numDue', 'numDoctors'] %}
                                                    <div class="d-flex justify-content-between align-items-center bg-slate-50 p-1 px-2 rounded border border-slate-100">
                                                        <span class="text-xxs font-bold text-slate-600">{{ form[field].vars.label }}</span>
                                                        {{ form_widget(form[field], {'attr': {'class': 'form-control !h-6 !w-12 !p-0 text-center text-xxs', 'type': 'number', 'min': '0'}}) }}
                                                    </div>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        <!-- Ambulancias -->
                                        <div class="col-6">
                                            <span class="text-xxs font-black text-slate-400 uppercase tracking-tighter d-block mb-2">Tipo Ambulancia</span>
                                            <div class="space-y-1">
                                                {% for field in ['numSvb', 'numSva', 'numColectiva'] %}
                                                    <div class="d-flex justify-content-between align-items-center bg-slate-50 p-1 px-2 rounded border border-slate-100">
                                                        <span class="text-xxs font-bold text-slate-600">{{ form[field].vars.label }}</span>
                                                        {{ form_widget(form[field], {'attr': {'class': 'form-control !h-6 !w-12 !p-0 text-center text-xxs', 'type': 'number', 'min': '0'}}) }}
                                                    </div>
                                                {% endfor %}
                                            </div>
                                        </div>
                                        <!-- Vehículos (Compact list) -->
                                        <div class="col-12 mt-2">
                                            <span class="text-xxs font-black text-slate-400 uppercase tracking-tighter d-block mb-1">Asignar Vehículos</span>
                                            <div class="overflow-y-auto" style="max-height: 200px;">
                                                {% for key, vehicle_choice in form.vehicles %}
                                                    {% set vehicle = form.vehicles.vars.choices[key].data %}
                                                    <div class="d-flex align-items-center justify-content-between py-1 border-bottom border-slate-100">
                                                        <span class="text-xxs text-slate-700">{{ vehicle.alias ?: vehicle.model }} <small class="text-muted">({{ vehicle.licensePlate }})</small></span>
                                                        <div class="form-check form-switch m-0 p-0">
                                                            <input class="form-check-input ms-0" type="checkbox" name="{{ vehicle_choice.vars.full_name }}" id="{{ vehicle_choice.vars.id }}" value="{{ vehicle_choice.vars.value }}" {% if vehicle_choice.vars.checked %}checked{% endif %}>
                                                        </div>
                                                    </div>
                                                {% endfor %}
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB 3: ASISTENCIA -->
        <div class="tab-pane fade" id="asistencia" role="tabpanel" aria-labelledby="asistencia-tab">
            <div class="bg-white shadow-sm border border-slate-200 rounded-3 p-4">
                <div class="flex justify-between items-center mb-6 pb-4 border-b border-slate-100">
                    <h3 class="text-lg font-bold text-slate-800 flex items-center">
                        <i data-lucide="user-check" class="w-5 h-5 mr-2 text-green-600"></i> Gestión de Asistencia
                    </h3>
                    <div class="flex space-x-2">
                        {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_COORDINATOR') %}
                        <button type="button" data-action="click->service-form#openModal" class="btn btn-primary btn-sm flex items-center">
                            <i data-lucide="user-plus" class="mr-2 h-4 w-4"></i> Añadir voluntarios
                        </button>
                        {% endif %}
                        <button type="button" data-action="click->service-form#ficharTodos" class="btn btn-outline-success btn-sm flex items-center">
                            <i data-lucide="check-square" class="mr-2 h-4 w-4"></i> Fichar todos
                        </button>
                    </div>
                </div>

                <div class="row g-4">
                    <!-- Asisten -->
                    <div class="col-md-4">
                        <div class="bg-slate-50 rounded-2xl p-4 border border-slate-100 h-100">
                            <h4 class="text-xs font-black uppercase tracking-wider text-green-700 mb-4 flex justify-between items-center">
                                <span>Asistentes</span>
                                <span class="bg-green-100 px-2 py-0.5 rounded-full text-xxs">{{ service.assistanceConfirmations|filter(c => c.status == 'attending')|length }}</span>
                            </h4>
                            <ul class="space-y-2">
                                {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'attending') %}
                                    <li class="bg-white p-3 rounded-xl shadow-sm border border-slate-100 group">
                                        {% set vs = fichajes[confirmation.volunteer.id] ?? null %}
                                        <div class="flex items-center justify-between">
                                            <div class="flex-grow">
                                                <div class="flex items-center gap-2 mb-1">
                                                    <p class="text-sm font-bold text-slate-800 m-0">
                                                        {{ confirmation.volunteer.name }} {{ confirmation.volunteer.lastname }}
                                                    </p>
                                                    <div class="flex items-center gap-1">
                                                        <button type="button" class="btn p-0 border-0 {{ confirmation.isFichajeResponsible ? 'text-yellow-500' : 'text-slate-200' }}" title="Responsable">
                                                            <i data-lucide="star" class="h-3 w-3 fill-current"></i>
                                                        </button>
                                                        <button type="button" class="btn p-0 border-0 {{ confirmation.isSanitario ? 'text-blue-500' : 'text-slate-200' }}" title="Sanitario">
                                                            <i data-lucide="cross" class="h-3 w-3"></i>
                                                        </button>
                                                        <button type="button" class="btn p-0 border-0 {{ confirmation.isSocorrista ? 'text-orange-500' : 'text-slate-200' }}" title="Socorrista">
                                                            <i data-lucide="life-buoy" class="h-3 w-3"></i>
                                                        </button>
                                                        <button type="button" class="btn p-0 border-0 {{ confirmation.isConductor ? 'text-green-500' : 'text-slate-200' }}" title="Conductor">
                                                            <i data-lucide="truck" class="h-3 w-3"></i>
                                                        </button>
                                                    </div>
                                                </div>
                                                {% if vs %}
                                                    {% set totalMinutes = vs.calculateTotalDuration() %}
                                                    {% if totalMinutes > 0 %}
                                                        {% set hours = (totalMinutes / 60)|round(0, 'floor') %}
                                                        {% set minutes = totalMinutes % 60 %}
                                                        <span class="text-xxs font-bold text-slate-400 uppercase tracking-tight">{{ hours }}h {{ minutes }}m totales</span>
                                                    {% endif %}
                                                {% endif %}
                                                <div class="mt-2">
                                                    <input type="text" name="justification" value="{{ confirmation.justification }}"
                                                           placeholder="Motivo / Detalle horas..."
                                                           class="form-control form-control-sm text-[10px] bg-slate-50 border-transparent focus:bg-white focus:border-slate-200 py-0 px-2 h-6 rounded-md"
                                                           data-action="change->service-form#updateJustification"
                                                           data-confirmation-id="{{ confirmation.id }}">
                                                </div>
                                            </div>
                                            <div class="flex items-center space-x-1 opacity-0 group-hover:opacity-100 transition-opacity">
                                                {% if vs %}
                                                    {% set last_end_time = null %}
                                                    {% for fichaje_item in vs.fichajes %}
                                                        {% if fichaje_item.endTime is not null %}
                                                            {% if last_end_time is null or fichaje_item.endTime > last_end_time %}
                                                                {% set last_end_time = fichaje_item.endTime %}
                                                            {% endif %}
                                                        {% endif %}
                                                    {% endfor %}
                                                    <button type="button"
                                                            class="p-1 rounded hover:bg-blue-50 text-blue-400"
                                                            title="Añadir fichaje manual"
                                                            data-action="click->service-form#openIndividualFichajeModal"
                                                            data-volunteer-service-id="{{ vs.id }}"
                                                            data-volunteer-name="{{ confirmation.volunteer.name }} {{ confirmation.volunteer.lastname }}"
                                                            data-last-clock-out="{{ last_end_time ? last_end_time|date('Y-m-d H:i:s') : '' }}">
                                                        <i data-lucide="clock" class="h-3.5 w-3.5"></i>
                                                    </button>
                                                {% endif %}
                                                <div class="dropdown">
                                                    <button class="p-1 rounded hover:bg-slate-100 text-slate-400" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                                                        <i data-lucide="shield" class="h-3.5 w-3.5"></i>
                                                    </button>
                                                    <ul class="dropdown-menu dropdown-menu-end shadow-sm">
                                                        <li><h6 class="dropdown-header text-xxs uppercase font-black">Asignar Rol</h6></li>
                                                        <li>
                                                            <form method="post" action="{{ path('app_assistance_confirmation_toggle_role', {'id': confirmation.id, 'role': 'responsible'}) }}">
                                                                <input type="hidden" name="_token" value="{{ csrf_token('toggle-role' ~ confirmation.id) }}">
                                                                <button type="submit" class="dropdown-item text-xs flex items-center gap-2">
                                                                    <i data-lucide="star" class="w-3 h-3 {{ confirmation.isFichajeResponsible ? 'text-yellow-500 fill-current' : '' }}"></i> Responsable
                                                                </button>
                                                            </form>
                                                        </li>
                                                        <li>
                                                            <form method="post" action="{{ path('app_assistance_confirmation_toggle_role', {'id': confirmation.id, 'role': 'sanitario'}) }}">
                                                                <input type="hidden" name="_token" value="{{ csrf_token('toggle-role' ~ confirmation.id) }}">
                                                                <button type="submit" class="dropdown-item text-xs flex items-center gap-2">
                                                                    <i data-lucide="cross" class="w-3 h-3 {{ confirmation.isSanitario ? 'text-blue-500' : '' }}"></i> Sanitario
                                                                </button>
                                                            </form>
                                                        </li>
                                                        <li>
                                                            <form method="post" action="{{ path('app_assistance_confirmation_toggle_role', {'id': confirmation.id, 'role': 'socorrista'}) }}">
                                                                <input type="hidden" name="_token" value="{{ csrf_token('toggle-role' ~ confirmation.id) }}">
                                                                <button type="submit" class="dropdown-item text-xs flex items-center gap-2">
                                                                    <i data-lucide="life-buoy" class="w-3 h-3 {{ confirmation.isSocorrista ? 'text-orange-500' : '' }}"></i> Socorrista
                                                                </button>
                                                            </form>
                                                        </li>
                                                        <li>
                                                            <form method="post" action="{{ path('app_assistance_confirmation_toggle_role', {'id': confirmation.id, 'role': 'conductor'}) }}">
                                                                <input type="hidden" name="_token" value="{{ csrf_token('toggle-role' ~ confirmation.id) }}">
                                                                <button type="submit" class="dropdown-item text-xs flex items-center gap-2">
                                                                    <i data-lucide="truck" class="w-3 h-3 {{ confirmation.isConductor ? 'text-green-500' : '' }}"></i> Conductor
                                                                </button>
                                                            </form>
                                                        </li>
                                                    </ul>
                                                </div>
                                                <form method="post" action="{{ path('app_assistance_confirmation_remove', {'id': confirmation.id}) }}"
                                                      data-controller="confirm"
                                                      data-confirm-text-value="¿Deseas eliminar la confirmación de asistencia de {{ confirmation.volunteer.name }}?"
                                                      data-confirm-title-value="Eliminar Asistencia"
                                                      data-confirm-confirm-button-text-value="Eliminar">
                                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confirmation.id) }}">
                                                    <button type="button" data-action="click->confirm#confirm" class="p-1 rounded hover:bg-red-50 text-red-400" title="Eliminar">
                                                        <i data-lucide="trash-2" class="h-3.5 w-3.5"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </div>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>

                    <!-- Reserva -->
                    <div class="col-md-4">
                        <div class="bg-slate-50 rounded-2xl p-4 border border-slate-100 h-100">
                            <h4 class="text-xs font-black uppercase tracking-wider text-orange-700 mb-4 flex justify-between items-center">
                                <span>En Reserva</span>
                                <span class="bg-orange-100 px-2 py-0.5 rounded-full text-xxs">{{ service.assistanceConfirmations|filter(c => c.status == 'reserved')|length }}</span>
                            </h4>
                            <ul class="space-y-2">
                                {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'reserved') %}
                                    <li class="bg-white p-3 rounded-xl shadow-sm border border-slate-100 flex justify-between items-center">
                                        <span class="text-sm font-bold text-slate-700">{{ confirmation.volunteer.name }} {{ confirmation.volunteer.lastname }}</span>
                                        <form method="post" action="{{ path('app_assistance_confirmation_remove', {'id': confirmation.id}) }}"
                                              data-controller="confirm"
                                              data-confirm-text-value="¿Deseas eliminar a {{ confirmation.volunteer.name }} de la reserva?">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confirmation.id) }}">
                                            <button type="button" data-action="click->confirm#confirm" class="text-slate-300 hover:text-red-500 transition-colors">
                                                <i data-lucide="trash-2" class="h-4 w-4"></i>
                                            </button>
                                        </form>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>

                    <!-- No asisten -->
                    <div class="col-md-4">
                        <div class="bg-slate-50 rounded-2xl p-4 border border-slate-100 h-100">
                            <h4 class="text-xs font-black uppercase tracking-wider text-red-700 mb-4 flex justify-between items-center">
                                <span>Bajas</span>
                                <span class="bg-red-100 px-2 py-0.5 rounded-full text-xxs">{{ service.assistanceConfirmations|filter(c => c.status == 'not_attending')|length }}</span>
                            </h4>
                            <ul class="space-y-2">
                                {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'not_attending') %}
                                    <li class="bg-white p-3 rounded-xl shadow-sm border border-slate-100 flex justify-between items-center">
                                        <span class="text-sm text-slate-500 line-through">{{ confirmation.volunteer.name }} {{ confirmation.volunteer.lastname }}</span>
                                        <form method="post" action="{{ path('app_assistance_confirmation_remove', {'id': confirmation.id}) }}"
                                              data-controller="confirm"
                                              data-confirm-text-value="¿Deseas eliminar a {{ confirmation.volunteer.name }} del listado de bajas?">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confirmation.id) }}">
                                            <button type="button" data-action="click->confirm#confirm" class="text-slate-300 hover:text-red-500 transition-colors">
                                                <i data-lucide="trash-2" class="h-4 w-4"></i>
                                            </button>
                                        </form>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>

                    <!-- No asisten -->
                    <div class="col-md-4">
                        <div class="bg-slate-50 rounded-2xl p-4 border border-slate-100 h-100">
                            <h4 class="text-xs font-black uppercase tracking-wider text-red-700 mb-4 flex justify-between items-center">
                                <span>Bajas</span>
                                <span class="bg-red-100 px-2 py-0.5 rounded-full text-xxs">{{ service.assistanceConfirmations|filter(c => c.status == 'not_attending')|length }}</span>
                            </h4>
                            <ul class="space-y-2">
                                {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'not_attending') %}
                                    <li class="bg-white p-3 rounded-xl shadow-sm border border-slate-100 flex justify-between items-center">
                                        <span class="text-sm text-slate-500 line-through">{{ confirmation.volunteer.name }} {{ confirmation.volunteer.lastname }}</span>
                                        <form method="post" action="{{ path('app_assistance_confirmation_remove', {'id': confirmation.id}) }}">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ confirmation.id) }}">
                                            <button type="submit" class="text-slate-300 hover:text-red-500 transition-colors">
                                                <i data-lucide="trash-2" class="h-4 w-4"></i>
                                            </button>
                                        </form>
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- TAB 4: VEHICULOS Y DOTACION -->
        <div class="tab-pane fade" id="vehiculos_dotacion" role="tabpanel" aria-labelledby="vehiculos-tab">
            <div class="card border-0 shadow-sm rounded-3 p-4">
                <h3 class="text-lg font-bold text-slate-800 mb-4 flex items-center">
                    <i data-lucide="truck" class="w-5 h-5 mr-2 text-blue-600"></i> Vehículos y Dotaciones
                </h3>
                <div class="row g-4">
                    {% for vehicle in service.vehicles %}
                        <div class="col-md-6 col-lg-4">
                            <div class="bg-slate-50 border border-slate-200 rounded-2xl p-4 h-100">
                                <div class="flex items-center justify-between mb-4">
                                    <div class="flex flex-col">
                                        <span class="text-sm font-black text-slate-900">{{ vehicle.alias ?: vehicle.model }}</span>
                                        <span class="text-xxs font-mono text-slate-400">{{ vehicle.licensePlate }}</span>
                                    </div>
                                    <i data-lucide="truck" class="w-5 h-5 text-slate-300"></i>
                                </div>
                                <div class="space-y-3">
                                    <div class="field-group !mb-0">
                                        <label class="text-[10px] font-black uppercase text-slate-400 tracking-wider mb-1 block">Conductor</label>
                                        <select class="form-select form-select-sm rounded-lg border-slate-200"
                                                data-action="change->service-form#assignVehicleCrew"
                                                data-vehicle-id="{{ vehicle.id }}"
                                                data-role="conductor">
                                            <option value="">Selecciona...</option>
                                            {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'attending') %}
                                                <option value="{{ confirmation.volunteer.id }}" {{ (confirmation.assignedVehicle == vehicle and confirmation.vehicleRole == 'conductor') ? 'selected' : '' }}>
                                                    {{ confirmation.volunteer.name }}
                                                </option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                    <div class="field-group !mb-0">
                                        <label class="text-[10px] font-black uppercase text-slate-400 tracking-wider mb-1 block">Copiloto</label>
                                        <select class="form-select form-select-sm rounded-lg border-slate-200"
                                                data-action="change->service-form#assignVehicleCrew"
                                                data-vehicle-id="{{ vehicle.id }}"
                                                data-role="copiloto">
                                            <option value="">Selecciona...</option>
                                            {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'attending') %}
                                                <option value="{{ confirmation.volunteer.id }}" {{ (confirmation.assignedVehicle == vehicle and confirmation.vehicleRole == 'copiloto') ? 'selected' : '' }}>
                                                    {{ confirmation.volunteer.name }}
                                                </option>
                                            {% endfor %}
                                        </select>
                                    </div>
                                    <div class="field-group !mb-0">
                                        <label class="text-[10px] font-black uppercase text-slate-400 tracking-wider mb-1 block">Acompañantes</label>
                                        <div class="bg-white border border-slate-200 rounded-lg p-2 max-h-32 overflow-y-auto">
                                            {% for confirmation in service.assistanceConfirmations|filter(c => c.status == 'attending') %}
                                                <div class="form-check text-xxs">
                                                    <input class="form-check-input" type="checkbox"
                                                           value="{{ confirmation.volunteer.id }}"
                                                           id="v_{{ vehicle.id }}_{{ confirmation.id }}"
                                                           data-action="change->service-form#assignVehicleCrew"
                                                           data-vehicle-id="{{ vehicle.id }}"
                                                           data-role="acompañante"
                                                           {{ (confirmation.assignedVehicle == vehicle and confirmation.vehicleRole == 'acompañante') ? 'checked' : '' }}>
                                                    <label class="form-check-label" for="v_{{ vehicle.id }}_{{ confirmation.id }}">
                                                        {{ confirmation.volunteer.name }}
                                                    </label>
                                                </div>
                                            {% endfor %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {% else %}
                        <div class="col-12">
                            <div class="text-center py-12 bg-slate-50 rounded-2xl border border-dashed border-slate-300">
                                <i data-lucide="truck" class="w-12 h-12 text-slate-300 mx-auto mb-4"></i>
                                <p class="text-slate-500 font-medium">No hay vehículos asignados a este servicio.</p>
                                <p class="text-xs text-slate-400">Puedes asignarlos en la pestaña de 'Recursos y Dotación'.</p>
                            </div>
                        </div>
                    {% endfor %}
                </div>
            </div>
        </div>

        <!-- TAB 5: REGISTRO Y PARTES -->
        <div class="tab-pane fade" id="registro_partes" role="tabpanel" aria-labelledby="registro-tab">
            <div class="card border-0 shadow-sm rounded-3 p-4">
                <ul class="nav nav-pills mb-4 gap-2" id="partes-tab" role="tablist">
                    <li class="nav-item">
                        <button class="nav-link active px-4 py-2 rounded-xl font-bold text-xs uppercase transition-all" id="intervenciones-tab" data-bs-toggle="pill" data-bs-target="#intervenciones" type="button" role="tab">Intervenciones</button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link px-4 py-2 rounded-xl font-bold text-xs uppercase transition-all" id="incendios-tab" data-bs-toggle="pill" data-bs-target="#incendios" type="button" role="tab">Incendios</button>
                    </li>
                    <li class="nav-item">
                        <button class="nav-link px-4 py-2 rounded-xl font-bold text-xs uppercase transition-all" id="comms-tab" data-bs-toggle="pill" data-bs-target="#comms" type="button" role="tab">Comunicaciones</button>
                    </li>
                </ul>
                <div class="tab-content border-top pt-4">
                    <div class="tab-pane fade show active" id="intervenciones" role="tabpanel">
                        <div class="text-center py-8">
                            <i data-lucide="activity" class="w-8 h-8 text-slate-300 mx-auto mb-2"></i>
                            <p class="text-slate-400 text-sm font-medium italic">Estructura preparada para Intervenciones</p>
                        </div>
                    </div>
                    <div class="tab-pane fade" id="incendios" role="tabpanel">
                        <div class="text-center py-8">
                            <i data-lucide="flame" class="w-8 h-8 text-slate-300 mx-auto mb-2"></i>
                            <p class="text-slate-400 text-sm font-medium italic">Estructura preparada para Incendios</p>
                        </div>
                    </div>
                    <div class="tab-pane fade" id="comms" role="tabpanel">
                        <div class="text-center py-8">
                            <i data-lucide="radio" class="w-8 h-8 text-slate-300 mx-auto mb-2"></i>
                            <p class="text-slate-400 text-sm font-medium italic">Estructura preparada para Comunicaciones</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Hidden Fields -->
    <div style="display:none;">
        {{ form_rest(form) }}
    </div>

    {{ form_end(form) }}

    <!-- Modals -->
    <!-- Attendance Volunteer Picker Modal -->
    <div class="hidden fixed inset-0 bg-slate-900/60 backdrop-blur-sm flex items-center justify-center z-50" style="display: none;" data-service-form-target="modal">
        <div class="bg-white rounded-3xl shadow-2xl p-8 max-w-2xl w-full mx-4 relative">
            <div class="flex justify-between items-center mb-6 border-b pb-4">
                <div class="flex flex-col flex-1">
                    <h3 class="text-2xl font-bold text-slate-900">Añadir Voluntarios</h3>
                    <button type="button" data-action="click->service-form#triggerFicharTodos" class="text-xs font-bold text-blue-600 hover:text-blue-800 flex items-center mt-1">
                        <i data-lucide="check-square" class="w-3 h-3 mr-1"></i> Fichar a todos simultáneamente
                    </button>
                </div>
                <div class="flex-1 px-4">
                    <div class="relative">
                        <i data-lucide="search" class="absolute left-4 top-1/2 -translate-y-1/2 w-4 h-4 text-slate-400"></i>
                        <input type="text" id="attendance_user_search" data-service-form-target="userSearchInput" data-action="keyup->service-form#search input->service-form#clearError" placeholder="Búsqueda predictiva..." class="form-control pl-10 rounded-xl border-slate-200 bg-slate-50 focus:bg-white text-sm">
                    </div>
                </div>
                <button type="button" data-action="click->service-form#closeModal" class="text-slate-400 hover:text-slate-600 p-2">
                    <i data-lucide="x" class="h-6 w-6"></i>
                </button>
            </div>

            <div class="mb-4">
                <label class="block text-sm font-bold text-slate-700 mb-2">Estado de Inscripción</label>
                <select id="attendance_status_select" data-service-form-target="attendanceStatusSelect" class="form-select w-full rounded-xl border-slate-200" data-action="change->service-form#clearError">
                    <option value="" selected disabled>Selecciona...</option>
                    <option value="attends">Confirmar Asistencia</option>
                    <option value="not_attends">Confirmar Baja</option>
                </select>
                <div class="invalid-feedback hidden" id="error-attendance_status_select"></div>
            </div>

            <div data-service-form-target="userList" class="space-y-1 mb-6 border border-slate-100 rounded-xl p-3 max-h-60 overflow-y-auto bg-slate-50 shadow-inner">
                <!-- List populated via JS -->
            </div>

            <div class="flex justify-between items-center border-t pt-6">
                <div data-service-form-target="paginationSummary" class="text-xs font-bold text-slate-400"></div>
                <div data-service-form-target="paginationContainer" class="flex gap-2"></div>
                <button type="button" data-action="click->service-form#saveAttendance" class="btn btn-primary px-8">Guardar Cambios</button>
            </div>
        </div>
    </div>

    {{ include('service/_fichar_modal.html.twig') }}
    {{ include('service/_individual_fichaje_modal.html.twig') }}

    <!-- Type Modal -->
    <div id="typeModal" class="fixed inset-0 bg-gray-800/60 backdrop-blur-sm flex items-center justify-center z-50 hidden" style="display: none;" data-service-form-target="typeModal">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md relative">
            <div class="flex justify-between items-center border-b pb-3 mb-4">
                <h3 class="text-xl font-semibold text-gray-700">Añadir Nuevo Tipo</h3>
                <button type="button" data-action="click->service-form#closeTypeModal" class="text-gray-400 hover:text-gray-600">
                    <i data-lucide="x" class="h-6 w-6"></i>
                </button>
            </div>
            <form id="modal-new-type-form" data-action="submit->service-form#handleTypeSubmit" novalidate>
                <div class="mb-4">
                    <label for="type_name_input" class="block text-sm font-medium text-gray-700 mb-1">Nombre <span class="text-red-500">*</span></label>
                    <input type="text" id="type_name_input" name="name" class="form-control w-full border-gray-300 rounded-md shadow-sm" required data-action="input->service-form#clearError">
                    <div class="invalid-feedback hidden" id="error-type_name_input"></div>
                </div>
                <div class="mt-6 flex justify-end">
                    <button type="submit" class="btn btn-primary">Aceptar</button>
                </div>
            </form>
        </div>
    </div>

    <!-- Subcategory Modal -->
    <div id="subcategoryModal" class="fixed inset-0 bg-gray-800/60 backdrop-blur-sm flex items-center justify-center z-50 hidden" style="display: none;" data-service-form-target="subcategoryModal">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-md relative">
            <div class="flex justify-between items-center border-b pb-3 mb-4">
                <h3 class="text-xl font-semibold text-gray-700">Añadir Categoría/Subcategoría</h3>
                <button type="button" data-action="click->service-form#closeSubcategoryModal" class="text-gray-400 hover:text-gray-600">
                    <i data-lucide="x" class="h-6 w-6"></i>
                </button>
            </div>
            <form id="modal-new-subcategory-form" data-action="submit->service-form#handleSubcategorySubmit" novalidate>
                <input type="hidden" id="modal_type_id" name="type_id">

                <div class="mb-4">
                    <label for="modal_category_select" class="block text-sm font-medium text-gray-700 mb-1">Categoría (Nivel 2) <span class="text-red-500">*</span></label>
                    <div class="input-group">
                        <select id="modal_category_select" name="category" class="form-select w-full border-gray-300 rounded-md shadow-sm" required data-action="change->service-form#clearError">
                            <option value="">Selecciona...</option>
                        </select>
                        <button type="button" class="btn btn-outline-primary" data-action="click->service-form#showNewCategoryInput">
                            <i data-lucide="plus" class="w-4 h-4"></i>
                        </button>
                    </div>
                    <div class="invalid-feedback hidden" id="error-modal_category_select"></div>
                </div>

                <div id="new_category_input_container" class="mb-4 hidden">
                    <label for="modal_new_category_name" class="block text-sm font-medium text-gray-700 mb-1">Nueva Categoría</label>
                    <input type="text" id="modal_new_category_name" name="new_category_name" class="form-control w-full border-gray-300 rounded-md shadow-sm" data-action="input->service-form#clearError">
                    <div class="invalid-feedback hidden" id="error-modal_new_category_name"></div>
                </div>

                <div class="mb-4">
                    <label for="subcategory_name_input" class="block text-sm font-medium text-gray-700 mb-1">Subcategoría (Nivel 3) <span class="text-red-500">*</span></label>
                    <input type="text" id="subcategory_name_input" name="name" class="form-control w-full border-gray-300 rounded-md shadow-sm" required data-action="input->service-form#clearError">
                    <div class="invalid-feedback hidden" id="error-subcategory_name_input"></div>
                </div>

                <div class="mt-6 flex justify-end">
                    <button type="submit" class="btn btn-primary">Aceptar</button>
                </div>
            </form>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        document.addEventListener('turbo:load', () => {
            const ficharTodosBtn = document.getElementById('fichar-todos-btn');
            const ficharModal = document.getElementById('fichar-modal');

            if (ficharTodosBtn && ficharModal) {
                ficharTodosBtn.addEventListener('click', () => {
                    const startDate = new Date("{{ service.startDate|date('Y-m-d H:i:s') }}");
                    const endDate = new Date("{{ service.endDate|date('Y-m-d H:i:s') }}");

                    document.getElementById('start-date').value = startDate.toISOString().split('T')[0];
                    document.getElementById('start-time').value = startDate.toTimeString().substring(0, 5);
                    document.getElementById('end-date').value = endDate.toISOString().split('T')[0];
                    document.getElementById('end-time').value = endDate.toTimeString().substring(0, 5);

                    ficharModal.classList.remove('hidden');
                });
            }
        });
    </script>
{% endblock %}
