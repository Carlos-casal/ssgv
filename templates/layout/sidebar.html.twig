<!-- Sidebar -->
<div class="w-[260px] h-full bg-blue-600 text-white flex flex-col flex-shrink-0 m-0 p-0 overflow-y-auto" id="sidebar">

    <!-- Header -->
<div class="pl-[15px] border-b border-blue-700">
    {% if app.user %}
        <div class="flex items-start justify-between">
            <div class="flex items-center gap-3">
                <img src="{{ asset('uploads/profile_pictures/' ~ (app.user.volunteer.profilePicture|default('default.png'))) }}" alt="Foto de perfil" class="w-8 h-8 rounded-full object-cover flex-shrink-0">
                <div class="flex-1">
                    <p class="font-medium text-sm">{{ app.user.volunteer.name|default('Usuario') }}</p>
                    <p class="text-xs text-blue-300">{{ app.user.email }}</p>
                </div>
            </div>
            <div class="relative">
                <button class="flex items-center justify-center w-8 h-8 hover:bg-blue-700 rounded-full transition-colors">
                    <i data-lucide="bell" class="w-5 h-5"></i>
                </button>
                {% if pending_volunteer_count > 0 %}
                    <span class="absolute top-0 right-0 w-2 h-2 bg-red-500 rounded-full"></span>
                {% endif %}
            </div>
        </div>
        <div class="mt-3">
            <a href="{{ path('app_logout') }}" class="w-full flex items-center gap-3 px-3 py-2 text-red-400 hover:bg-red-500 hover:text-white rounded-lg transition-colors">
                <i data-lucide="log-out" class="w-5 h-5 flex-shrink-0"></i>
                <span class="text-sm">Cerrar Sesión</span>
            </a>
        </div>
    {% else %}
        <div class="flex items-center justify-end h-16"> {# Set a fixed height to avoid layout shift #}
            <div class="relative">
                <button class="flex items-center justify-center w-8 h-8 hover:bg-blue-700 rounded-full transition-colors">
                    <i data-lucide="bell" class="w-5 h-5"></i>
                </button>
                {% if pending_volunteer_count > 0 %}
                    <span class="absolute top-0 right-0 w-2 h-2 bg-red-500 rounded-full"></span>
                {% endif %}
            </div>
        </div>
    {% endif %}
    </div>

    <!-- Navegación Principal -->
    <div class="flex-1 pl-[15px] no-scrollbar">
        <nav class="space-y-1" id="main-nav">
            <ul class="space-y-1">
                <li>
                    <a href="{{ path('app_dashboard') }}" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700">
                        <i data-lucide="home" class="w-5 h-5 flex-shrink-0"></i>
                        <span class="text-sm font-medium">INICIO</span>
                    </a>
                </li>

                {% if is_granted('ROLE_ADMIN') %}
                <!-- GESTIÓN -->
                <li class="has-submenu">
                    <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700" aria-haspopup="true" aria-expanded="false">
                        <i data-lucide="folder-kanban" class="w-5 h-5 flex-shrink-0"></i>
                        <span class="flex-1 text-sm font-medium">GESTIÓN</span>
                        <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                    </a>
                    <ul class="pl-4 space-y-1">
                        <!-- PERSONAL -->
                        <li class="has-submenu">
                            <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-800" aria-haspopup="true" aria-expanded="false">
                                <i data-lucide="users" class="w-5 h-5 flex-shrink-0"></i>
                                <span class="flex-1 text-sm">PERSONAL</span>
                                <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                            </a>
                            <ul class="pl-4 space-y-1">
                                <li><a href="{{ path('app_volunteer_list') }}" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">LISTADO</a></li>
                                <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">INFORMES</a></li>
                            </ul>
                        </li>
                        <!-- SERVICIOS -->
                        <li class="has-submenu">
                            <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-800" aria-haspopup="true" aria-expanded="false">
                                <i data-lucide="briefcase" class="w-5 h-5 flex-shrink-0"></i>
                                <span class="flex-1 text-sm">SERVICIOS</span>
                                <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                            </a>
                            <ul class="pl-4 space-y-1">
                                <li><a href="{{ path('app_services_list') }}" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">LISTADO</a></li>
                                <li><a href="{{ path('app_admin_import_hours') }}" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">IMPORTAR HORAS</a></li>
                                <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">INFORMES</a></li>
                                <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">CUADRANTE</a></li>
                            </ul>
                        </li>
                        <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="megaphone" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">COMUNICADO Y ALERTAS</span></a></li>
                        <!-- RECURSOS -->
                        <li class="has-submenu">
                            <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700" aria-haspopup="true" aria-expanded="false">
                                <i data-lucide="archive" class="w-5 h-5 flex-shrink-0"></i>
                                <span class="flex-1 text-sm font-medium">RECURSOS</span>
                                <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                            </a>
                            <ul class="pl-4 space-y-1">
                                <li><a href="{{ path('app_vehicle_index') }}" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-800"><i data-lucide="truck" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm">VEHICULO</span></a></li>
                                <!-- INVENTARIO -->
                                <li class="has-submenu">
                                    <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-800" aria-haspopup="true" aria-expanded="false">
                                        <i data-lucide="boxes" class="w-5 h-5 flex-shrink-0"></i>
                                        <span class="flex-1 text-sm">INVENTARIO</span>
                                        <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                                    </a>
                                    <ul class="pl-4 space-y-1">
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">ARTICULOS</a></li>
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">ALMACENES</a></li>
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">PROVEEDORES</a></li>
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">ENTRADAS-SALIDAS</a></li>
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">TRASPASO</a></li>
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">LOTE</a></li>
                                    </ul>
                                </li>
                                <!-- ARCE -->
                                <li class="has-submenu">
                                    <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-800" aria-haspopup="true" aria-expanded="false">
                                        <i data-lucide="puzzle" class="w-5 h-5 flex-shrink-0"></i>
                                        <span class="flex-1 text-sm">ARCE</span>
                                        <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                                    </a>
                                    <ul class="pl-4 space-y-1">
                                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-900">INFORMES</a></li>
                                    </ul>
                                </li>
                            </ul>
                        </li>
                        <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="fire-hydrant" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">HIDRANTES</span></a></li>
                        <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="user-plus" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">FILIACIONES</span></a></li>
                        <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="landmark" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">TESORERIA</span></a></li>
                        <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="check-square" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">TAREAS</span></a></li>
                    </ul>
                </li>
                <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="file-archive" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">GESDOC</span></a></li>
                <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="phone-forwarded" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">CENTRAL</span></a></li>
                <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="bar-chart-2" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">ESTADISTICAS</span></a></li>

                <!-- CONFIGURACION -->
                <li class="has-submenu">
                    <a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700" aria-haspopup="true" aria-expanded="false">
                        <i data-lucide="settings" class="w-5 h-5 flex-shrink-0"></i>
                        <span class="flex-1 text-sm font-medium">CONFIGURACION</span>
                        <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                    </a>
                    <ul class="pl-4 space-y-1">
                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-800">SISTEMAS</a></li>
                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-800">UTILIDADES</a></li>
                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-800">ADMINISTRACION</a></li>
                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-800">ENTIDAD</a></li>
                        <li><a href="#" class="block px-3 py-2 text-sm rounded-lg hover:bg-blue-800">SUSCRIPCION</a></li>
                    </ul>
                </li>

                <li><a href="#" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700"><i data-lucide="user-cog" class="w-5 h-5 flex-shrink-0"></i><span class="text-sm font-medium">USUARIO</span></a></li>
                {% else %}
                    {# Simplified menu for non-admins, e.g., ROLE_VOLUNTEER #}
                     <li>
                        <a href="{{ path('app_services_list') }}" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700">
                            <i data-lucide="briefcase" class="w-5 h-5 flex-shrink-0"></i>
                            <span class="text-sm font-medium">Servicios</span>
                        </a>
                    </li>
                    <li>
                        <a href="{{ path('app_my_services') }}" class="flex items-center gap-3 px-3 py-2 rounded-lg hover:bg-blue-700">
                            <i data-lucide="star" class="w-5 h-5 flex-shrink-0"></i>
                            <span class="text-sm font-medium">Mis servicios</span>
                        </a>
                    </li>

                    {% set is_responsible_for_any_service = false %}
                    {% if app.user.volunteer is defined and app.user.volunteer %}
                        {% for confirmation in app.user.volunteer.assistanceConfirmations %}
                            {% if confirmation.isFichajeResponsible %}
                                {% set is_responsible_for_any_service = true %}
                            {% endif %}
                        {% endfor %}
                    {% endif %}

                    {% if is_responsible_for_any_service %}
                        <li>
                            <a href="{{ path('app_responsible_services_list') }}" class="flex items-center gap-3 px-3 py-2 rounded-lg text-yellow-400 bg-blue-800 hover:bg-blue-900">
                                <i data-lucide="shield-check" class="w-5 h-5 flex-shrink-0"></i>
                                <span class="text-sm font-medium">Servicios Responsable</span>
                            </a>
                        </li>
                    {% endif %}
                {% endif %}
            </ul>
        </nav>
    </div>

</div>

<style>
    .has-submenu > ul {
        overflow: hidden;
        max-height: 0;
        transition: max-height 0.3s ease-in-out;
        background-color: rgba(0, 0, 0, 0.1); /* Slightly darker background for submenus */
    }
    .has-submenu.submenu-open > ul {
        max-height: 1000px; /* A large enough value to show all content */
    }
    .has-submenu.submenu-open > a > [data-lucide="chevron-right"] {
        transform: rotate(90deg);
    }
    .has-submenu ul a {
        color: white; /* Ensure text is white */
    }
    .has-submenu ul a:hover {
        background-color: #1e40af; /* A darker blue for hover */
    }
</style>

<script>
    function setupSidebarHover() {
        const nav = document.getElementById('main-nav');
        if (!nav) return;

        const closeSubmenu = (submenu) => {
            // Recursively close all descendant submenus that are open
            submenu.querySelectorAll('.submenu-open').forEach(openChild => {
                openChild.classList.remove('submenu-open');
                const childLink = openChild.querySelector('a[aria-expanded]');
                if (childLink) {
                    childLink.setAttribute('aria-expanded', 'false');
                }
            });

            // Close the submenu itself
            if (submenu.classList.contains('submenu-open')) {
                submenu.classList.remove('submenu-open');
                const link = submenu.querySelector('a[aria-expanded]');
                if (link) {
                    link.setAttribute('aria-expanded', 'false');
                }
            }
        };

        nav.querySelectorAll('.has-submenu').forEach(item => {
            if (item.dataset.hoverListenersAttached) {
                return;
            }
            item.dataset.hoverListenersAttached = 'true';

            let enterTimer;
            let leaveTimer;

            item.addEventListener('mouseenter', () => {
                clearTimeout(leaveTimer); // Cancel any pending close operations

                enterTimer = setTimeout(() => {
                    // Close sibling submenus
                    const parentUl = item.parentElement;
                    parentUl.querySelectorAll(':scope > .has-submenu').forEach(sibling => {
                        if (sibling !== item) {
                            closeSubmenu(sibling);
                        }
                    });

                    // Open this submenu
                    item.classList.add('submenu-open');
                    const link = item.querySelector('a[aria-expanded]');
                    if (link) {
                        link.setAttribute('aria-expanded', 'true');
                    }
                }, 100); // A small delay to prevent accidental opening
            });

            item.addEventListener('mouseleave', () => {
                clearTimeout(enterTimer); // Cancel any pending open operations

                leaveTimer = setTimeout(() => {
                    closeSubmenu(item);
                }, 200); // Delay to allow moving mouse to child submenu
            });
        });
    }

    document.addEventListener('turbo:load', setupSidebarHover);
    document.addEventListener('DOMContentLoaded', setupSidebarHover);
</script>