<div class="bg-white shadow-lg w-64 flex flex-col h-full" id="sidebar">
    <!-- Encabezado de la Barra Lateral -->
    <div class="p-4 border-b border-gray-200 flex items-center gap-3 bg-primary text-white">
        <div class="w-10 h-10 bg-primary-dark rounded-lg flex items-center justify-center flex-shrink-0">
            <i data-lucide="shield" class="w-6 h-6 text-white"></i>
        </div>
        <div>
            <h2 class="font-bold text-lg">SGV</h2>
            <p class="text-xs text-primary-light">Gestión Voluntarios</p>
        </div>
    </div>

    <!-- Controles del Encabezado Movidos -->
    <div class="p-4 border-b border-gray-200 space-y-4">
        <!-- Fecha -->
        <p class="text-sm text-gray-600 text-center">
            {{ "now"|date("l, d \\d\\e F \\d\\e Y") }}
        </p>

        <!-- Barra de Búsqueda -->
        <div class="relative">
            <i data-lucide="search" class="absolute left-3 top-1/2 transform -translate-y-1/2 w-4 h-4 text-gray-400"></i>
            <input
                type="text"
                placeholder="Buscar..."
                class="pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary focus:border-primary w-full"
            />
        </div>

        <!-- Notificaciones -->
        <button class="w-full flex items-center justify-center gap-2 p-2 text-gray-600 hover:text-primary hover:bg-primary-light rounded-lg transition-colors group">
            <i data-lucide="bell" class="w-5 h-5 text-gray-500 group-hover:text-white"></i>
            <span class="text-sm font-medium group-hover:text-white">Notificaciones</span>
            <span class="w-2 h-2 bg-accent rounded-full"></span>
        </button>
    </div>
    <!-- Navegación Principal -->
<div class="flex-1 p-2 overflow-y-auto" id="main-nav-wrapper">
    <nav class="space-y-1" id="main-nav">
        <ul class="space-y-1">
            {% macro nav_link(path, icon, label, is_active = false) %}
                <li>
                    <a href="{{ path(path) }}"
                       class="flex items-center gap-3 px-3 py-2 rounded-lg transition-colors group
                              {{ is_active
                                 ? 'bg-primary-light text-white font-semibold shadow-inner'
                                 : 'text-gray-600 hover:bg-primary hover:text-white'
                              }}">
                        <i data-lucide="{{ icon }}"
                           class="w-5 h-5 flex-shrink-0 {{ is_active ? 'text-white' : 'text-gray-400 group-hover:text-white' }}"></i>
                        <span class="text-sm font-medium">{{ label }}</span>
                    </a>
                </li>
            {% endmacro %}

            {% macro submenu_toggle(icon, label) %}
                <a href="#"
                   class="flex items-center gap-3 px-3 py-2 rounded-lg text-gray-600 hover:bg-primary hover:text-white group"
                   aria-haspopup="true" aria-expanded="false">
                    <i data-lucide="{{ icon }}" class="w-5 h-5 flex-shrink-0 text-gray-400 group-hover:text-white"></i>
                    <span class="flex-1 text-sm font-medium">{{ label }}</span>
                    <i data-lucide="chevron-right" class="w-4 h-4 transition-transform"></i>
                </a>
            {% endmacro %}

            {% macro submenu_item(path, label, is_active = false) %}
                 <li>
                    <a href="{{ path is not null ? path(path) : '#' }}"
                       class="block px-3 py-2 text-sm rounded-lg transition-colors
                              {{ is_active
                                 ? 'bg-primary-light text-white font-medium'
                                 : 'text-gray-500 hover:bg-primary-light hover:text-white'
                              }}">
                        {{ label }}
                    </a>
                </li>
            {% endmacro %}

            {{ _self.nav_link('app_dashboard', 'home', 'INICIO', app.request.attributes.get('_route') == 'app_dashboard') }}

            {% if is_granted('ROLE_ADMIN') %}
                <!-- GESTIÓN -->
                <li class="has-submenu">
                    {{ _self.submenu_toggle('folder-kanban', 'GESTIÓN') }}
                    <ul class="pl-4 space-y-1">
                        <!-- PERSONAL -->
                        <li class="has-submenu">
                            {{ _self.submenu_toggle('users', 'PERSONAL') }}
                            <ul class="pl-4 space-y-1">
                                {{ _self.submenu_item('app_volunteer_list', 'LISTADO', 'app_volunteer_list' in app.request.attributes.get('_route')) }}
                                {{ _self.submenu_item(null, 'INFORMES') }}
                            </ul>
                        </li>
                        <!-- SERVICIOS -->
                        <li class="has-submenu">
                            {{ _self.submenu_toggle('briefcase', 'SERVICIOS') }}
                            <ul class="pl-4 space-y-1">
                                {{ _self.submenu_item('app_services_list', 'LISTADO', 'app_services_list' in app.request.attributes.get('_route')) }}
                                {{ _self.submenu_item(null, 'INFORMES') }}
                                {{ _self.submenu_item(null, 'CUADRANTE') }}
                            </ul>
                        </li>
                        {{ _self.nav_link('#', 'megaphone', 'COMUNICADO Y ALERTAS') }}
                        <!-- RECURSOS -->
                        <li class="has-submenu">
                             {{ _self.submenu_toggle('archive', 'RECURSOS') }}
                            <ul class="pl-4 space-y-1">
                                 {{ _self.submenu_item('app_vehicle_index', 'VEHICULO', 'app_vehicle_index' in app.request.attributes.get('_route')) }}
                                <!-- INVENTARIO -->
                                <li class="has-submenu">
                                    {{ _self.submenu_toggle('boxes', 'INVENTARIO') }}
                                    <ul class="pl-4 space-y-1">
                                        {{ _self.submenu_item(null, 'ARTICULOS') }}
                                        {{ _self.submenu_item(null, 'ALMACENES') }}
                                        {{ _self.submenu_item(null, 'PROVEEDORES') }}
                                        {{ _self.submenu_item(null, 'ENTRADAS-SALIDAS') }}
                                        {{ _self.submenu_item(null, 'TRASPASO') }}
                                        {{ _self.submenu_item(null, 'LOTE') }}
                                    </ul>
                                </li>
                                <!-- ARCE -->
                                <li class="has-submenu">
                                     {{ _self.submenu_toggle('puzzle', 'ARCE') }}
                                    <ul class="pl-4 space-y-1">
                                        {{ _self.submenu_item(null, 'INFORMES') }}
                                    </ul>
                                </li>
                            </ul>
                        </li>
                        {{ _self.nav_link('#', 'fire-hydrant', 'HIDRANTES') }}
                        {{ _self.nav_link('#', 'user-plus', 'FILIACIONES') }}
                        {{ _self.nav_link('#', 'landmark', 'TESORERIA') }}
                        {{ _self.nav_link('#', 'check-square', 'TAREAS') }}
                    </ul>
                </li>
                {{ _self.nav_link('#', 'file-archive', 'GESDOC') }}
                {{ _self.nav_link('#', 'phone-forwarded', 'CENTRAL') }}
                {{ _self.nav_link('#', 'bar-chart-2', 'ESTADISTICAS') }}

                <!-- CONFIGURACION -->
                <li class="has-submenu">
                     {{ _self.submenu_toggle('settings', 'CONFIGURACION') }}
                    <ul class="pl-4 space-y-1">
                        {{ _self.submenu_item(null, 'SISTEMAS') }}
                        {{ _self.submenu_item(null, 'UTILIDADES') }}
                        {{ _self.submenu_item(null, 'ADMINISTRACION') }}
                        {{ _self.submenu_item(null, 'ENTIDAD') }}
                        {{ _self.submenu_item(null, 'SUSCRIPCION') }}
                    </ul>
                </li>

                {{ _self.nav_link('#', 'user-cog', 'USUARIO') }}
            {% else %}
                {# Simplified menu for non-admins #}
                {{ _self.nav_link('app_services_list', 'briefcase', 'Servicios', 'app_services_list' in app.request.attributes.get('_route')) }}
                {{ _self.nav_link('app_my_services', 'star', 'Mis servicios', 'app_my_services' in app.request.attributes.get('_route')) }}

                {% set is_responsible = app.user.volunteer and app.user.volunteer.assistanceConfirmations|filter(c => c.isFichajeResponsible)|length > 0 %}

                {% if is_responsible %}
                    <li>
                        <a href="{{ path('app_responsible_services_list') }}"
                           class="flex items-center gap-3 px-3 py-2 rounded-lg transition-colors group bg-accent-light text-accent-dark hover:bg-accent hover:text-white">
                            <i data-lucide="shield-check" class="w-5 h-5 flex-shrink-0"></i>
                            <span class="text-sm font-medium">Servicios Responsable</span>
                        </a>
                    </li>
                {% endif %}
            {% endif %}
        </ul>
    </nav>
</div>


    <!-- Perfil de Usuario y Cerrar Sesión -->
    <div class="p-4 border-t border-gray-200">
        <div class="flex items-center gap-3 mb-3">
            <img src="{{ asset('uploads/profile_pictures/' ~ (app.user.volunteer.profilePicture|default('default.png'))) }}" alt="Foto de perfil" class="w-10 h-10 rounded-full object-cover border-2 border-primary-light">
            <div class="flex-1">
                <p class="font-semibold text-sm text-gray-800">{{ app.user.volunteer.name|default('Usuario') }}</p>
                <p class="text-xs text-gray-500">{{ app.user.email }}</p>
            </div>
        </div>
        <a href="{{ path('app_logout') }}" class="w-full flex items-center justify-center gap-2 px-3 py-2 text-accent-dark bg-accent-light hover:bg-accent hover:text-white rounded-lg transition-colors group">
            <i data-lucide="log-out" class="w-5 h-5 flex-shrink-0 text-accent-dark group-hover:text-white"></i>
            <span class="text-sm font-medium">Cerrar Sesión</span>
        </a>
    </div>
</div>
<style>
    .has-submenu > ul {
        overflow: hidden;
        max-height: 0;
        transition: max-height 0.3s ease-in-out;
    }
    .has-submenu.submenu-open > ul {
        max-height: 1000px; /* A large enough value to show all content */
    }
    .has-submenu.submenu-open > a > [data-lucide="chevron-right"] {
        transform: rotate(90deg);
    }
</style>
<script>
    function setupSidebar() {
        const submenuItems = document.querySelectorAll('.has-submenu');

        submenuItems.forEach((item, index) => {
            if (item.dataset.sidebarListenersAttached) {
                return;
            }
            item.dataset.sidebarListenersAttached = 'true';

            const link = item.querySelector('a');
            const submenu = item.querySelector('ul');
            if (!link || !submenu) return;

            // Generate unique IDs for ARIA
            const submenuId = `submenu-${index}`;
            submenu.setAttribute('id', submenuId);
            link.setAttribute('aria-controls', submenuId);

            link.addEventListener('click', function(event) {
                if (this.getAttribute('href') === '#') {
                    event.preventDefault();
                }

                const parentLi = this.parentElement;
                const isExpanded = parentLi.classList.toggle('submenu-open');
                this.setAttribute('aria-expanded', isExpanded);

                // Close sibling submenus
                const parentUl = parentLi.parentElement;
                parentUl.querySelectorAll(':scope > .has-submenu').forEach(sibling => {
                    if (sibling !== parentLi) {
                        sibling.classList.remove('submenu-open');
                        const siblingLink = sibling.querySelector('a');
                        if (siblingLink) {
                            siblingLink.setAttribute('aria-expanded', 'false');
                        }
                        // Recursively close any open submenus within the sibling
                        sibling.querySelectorAll('.submenu-open').forEach(sub => {
                            sub.classList.remove('submenu-open');
                            const subLink = sub.querySelector('a');
                            // Ensure the link exists before setting the attribute
                            if (subLink && subLink.hasAttribute('aria-expanded')) {
                                subLink.setAttribute('aria-expanded', 'false');
                            }
                        });
                    }
                });
            });
        });
    }

    document.addEventListener('turbo:load', setupSidebar);
    document.addEventListener('DOMContentLoaded', setupSidebar);
</script>