{% extends 'base.html.twig' %}

{% block title %}Temporary Login{% endblock %}

{% block body %}
<div class="min-h-screen bg-gray-100 flex items-center justify-center">
    <div class="bg-white p-8 rounded-lg shadow-md text-center">
        <h1 class="text-2xl font-bold mb-6">Temporary Access</h1>
        <div class="space-y-4">
            <a href="{{ path('app_login_admin_temp') }}"
               class="inline-block w-full px-6 py-3 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition-colors">
                Entrar como Administrador
            </a>
            <a href="{{ path('app_login_volunteer_temp') }}"
               class="inline-block w-full px-6 py-3 bg-gray-600 text-white font-semibold rounded-lg hover:bg-gray-700 transition-colors">
                Entrar como Voluntario
            </a>
        </div>
    </div>
</div>
{% endblock %}
