{% extends 'layout/app.html.twig' %}

{% block title %}Nueva Revisión de Ubicación{% endblock %}

{% block page_title %}Nueva Revisión{% endblock %}

{% block content %}
<div class="container-fluid py-4">
    <div class="row justify-content-center">
        <div class="col-lg-7">
            <div class="card border-0 shadow-sm border-top border-4 border-primary">
                <div class="card-header bg-white py-3">
                    <h6 class="fw-bold mb-0">Formulario de Auditoría y Control de Calidad</h6>
                </div>
                <div class="card-body">
                    {{ form_start(form) }}
                        <div class="row">
                            <div class="col-md-8 mb-3">
                                {{ form_label(form.location) }}
                                {{ form_widget(form.location) }}
                            </div>
                            <div class="col-md-4 mb-3">
                                {{ form_label(form.reviewDate) }}
                                {{ form_widget(form.reviewDate) }}
                            </div>
                        </div>
                        <div class="mb-3">
                            {{ form_label(form.responsible) }}
                            {{ form_widget(form.responsible) }}
                        </div>
                        <div class="mb-3">
                            {{ form_label(form.result) }}
                            {{ form_widget(form.result) }}
                        </div>
                        <div class="mb-4">
                            {{ form_label(form.nextReviewDate) }}
                            {{ form_widget(form.nextReviewDate) }}
                        </div>
                        <div class="alert alert-info py-2 small d-flex align-items-center">
                            <i data-lucide="info" class="w-4 h-4 me-2"></i>
                            El sistema programará automáticamente la próxima alerta en 30 días si no se especifica otra fecha.
                        </div>
                        <div class="d-grid gap-2">
                            <button type="submit" class="btn btn-primary btn-lg shadow-sm">Registrar Revisión</button>
                            <a href="{{ path('app_warehouse_dashboard') }}" class="btn btn-outline-secondary">Cancelar</a>
                        </div>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
