{% extends 'layout/app.html.twig' %}

{% block page_title %}Panel del Voluntario{% endblock %}

{% block content %}
<div class="p-6 space-y-6">
    <!-- Welcome section -->
    <div class="bg-gradient-to-r from-primary-500 to-accent-500 rounded-xl p-6 text-white">
        <h2 class="text-2xl font-bold mb-2">¡Hola, {{ app.user.volunteer.name|default('Voluntario') }}!</h2>
        <p class="text-primary-100">
            Gracias por tu dedicación. Aquí puedes gestionar tus servicios y mantenerte al día.
        </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <!-- My Services -->
        <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-200 flex flex-col">
            <h3 class="text-lg font-semibold text-gray-900 mb-2">Mis Servicios</h3>
            <p class="text-gray-600 mb-4 flex-grow">Consulta el historial de servicios que has realizado y gestiona tus próximos compromisos.</p>
            <a href="{{ path('app_my_services') }}" class="inline-flex items-center justify-center gap-2 px-4 py-2 bg-accent-500 text-white rounded-lg hover:bg-accent-600 transition-colors">
                <i data-lucide="history" class="w-4 h-4"></i>
                <span>Ver mis servicios</span>
            </a>
        </div>

        <!-- Available Services -->
        <div class="bg-white rounded-xl p-6 shadow-sm border border-gray-200 flex flex-col">
            <h3 class="text-lg font-semibold text-gray-900 mb-2">Servicios Disponibles</h3>
            <p class="text-gray-600 mb-4 flex-grow">Explora los próximos servicios y apúntate a los que te interesen.</p>
            <a href="{{ path('app_service_calendar') }}" class="inline-flex items-center justify-center gap-2 px-4 py-2 bg-accent-500 text-white rounded-lg hover:bg-accent-600 transition-colors">
                <i data-lucide="calendar-plus" class="w-4 h-4"></i>
                <span>Ver calendario</span>
            </a>
        </div>
    </div>
</div>
{% endblock %}